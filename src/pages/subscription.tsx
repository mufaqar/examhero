import { SubscriptionModule } from "@/modules/imports";
import Head from "next/head";
import React from "react";

const Subscription = () => {
  return (
    <>
      <Head>
        <title>Subscription</title>
      </Head>
      <SubscriptionModule />
    </>
  );
};

export default Subscription;
